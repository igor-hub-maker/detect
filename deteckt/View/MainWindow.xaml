<Window x:Class="deteckt.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local1="clr-namespace:deteckt.ViewModel" d:DataContext="{d:DesignInstance Type=local1:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="detecktOpenCV" Height="450" Width="800">
    <Window.DataContext>
        <local1:MainWindowViewModel/>
    </Window.DataContext>
    <Grid Height="419" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="792">
        <Image Source="{Binding Image}" Margin="0,0,365,210"/>
        <ComboBox Margin="432,10,0,373"
                      Width="350"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Center"
                      DisplayMemberPath="Name"
                      ItemsSource="{Binding VideoDevices}"
                      SelectedItem="{Binding CurrentDevice}" Height="36" />
        <Button Margin="432,51,10,328" Content="Start" Command="{Binding StartSourceCommand}"/>
        <Button Margin="432,96,10,283" Content="Stop" Command="{Binding StopSourceCommand}"/>
        <Button Margin="432,141,10,238" Content="Snapshot" Command="{Binding SaveSnapshotCommand}"/>

    </Grid>

</Window>